<%= form_for(@river, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @river.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@river.errors.count, "error") %> prohibited this river from being saved:</h2>

      <ul>
      <% @river.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label 'Location (ex: an address or local landmark)', :class => "control-label"%>
    <div class="controls">
      <%= f.text_field :address, :class => "text_field"  %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :city, :class => "control-label"%>
    <div class="controls">
      <%= f.text_field :city, :class => "text_field"  %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :state, :class => "control-label"%>
    <div class="controls">
      <%= f.text_field :state, :class => "text_field"  %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :country, :class => "control-label"%>
    <div class="controls">
      <%= f.text_field :country, :class => "text_field"  %>
    </div>
  </div>


  
  <div class="form-actions">
    <%= f.submit %>
  </div>
<% end %>
