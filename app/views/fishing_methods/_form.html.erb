<%= form_for(@fishing_method) do |f| %>
  <% if @fishing_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fishing_method.errors.count, "error") %> prohibited this fishing_method from being saved:</h2>

      <ul>
      <% @fishing_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
