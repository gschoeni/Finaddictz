<ul class="nav pull-right">
  
  <%= render 'layouts/navbars/posts-dropdown' %>

  <li class="divider-vertical"></li>
  <li><%= link_to "Profile", @current_user %></li>
  <li class="divider-vertical"></li>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%=current_user%> 
      <% if current_user.notifications > 0 %>
        <span class="badge badge-important"><%=current_user.notifications %></span> 
      <% end %>
    </a>
    <ul class="dropdown-menu">
      <li>
        <a href="<%= conversations_url %>">
          Inbox
          <% if current_user.notifications > 0 %>
            <span class="badge badge-important"><%=current_user.notifications %></span> 
          <% end %>
        </a>
      </li>
      <%= render 'layouts/navbars/switch_roles' %>
      <li class="divider"></li>
      <li>
        <%= link_to "Logout", logout_url %>
      </li>
    </ul>
  </li>
</ul>